# 终端指令

## 激活环境
```test
cd /home/windsky/project/Clutter
conda activate clutter
```

## 运行Demograsp ckpt（GUI可视播放）
指令都用不了
```test
python scripts/play_ppo_onestep.py \
  --task Clutter-Grasp-Direct-v0 \
  --checkpoint assets/checkpoints/inspire.pt \
  --num_envs 1 \
  --episodes 10 \
  --device cuda:0 \
  --disable_fabric
```

最小 headless smoke 可以用：
```test
python scripts/play_ppo_onestep.py \
  --task Clutter-Grasp-Direct-v0 \
  --checkpoint assets/checkpoints/inspire.pt \
  --num_envs 1 \
  --episodes 1 \
  --headless \
  --device cuda:0 \
  --disable_fabric
```

批量评估可以用：
```test
python scripts/play_ppo_onestep.py \
  --task Clutter-Grasp-Direct-v0 \
  --checkpoint assets/checkpoints/inspire.pt \
  --num_envs 175 \
  --episodes 100 \
  --device cuda:0 \
  --disable_fabric
```

## 场景搭建指令
```test 

```

## RL train
```test 

```

## RL play
```test 

```
