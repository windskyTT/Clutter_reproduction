1. run_rl_grasp.py

已按迁移说明完成第 1 项 `run_rl_grasp.py` 的重写迁移，入口拆成：

- [train_ppo_onestep.py](/home/windsky/project/Clutter/scripts/train_ppo_onestep.py:1)：IsaacLab `AppLauncher`、`gymnasium.make("Clutter-Grasp-Direct-v0")`、训练日志写入 `logs/`、可加载 checkpoint。
- [play_ppo_onestep.py](/home/windsky/project/Clutter/scripts/play_ppo_onestep.py:1)：从 `assets/checkpoints/inspire.pt` 默认读取策略，也支持 `--checkpoint` 覆盖。
- [ppo.py](/home/windsky/project/Clutter/source/Clutter/Clutter/algo/ppo_onestep/ppo.py:1)、[module.py](/home/windsky/project/Clutter/source/Clutter/Clutter/algo/ppo_onestep/module.py:1)、[storage.py](/home/windsky/project/Clutter/source/Clutter/Clutter/algo/ppo_onestep/storage.py:1)：补齐 `Clutter.algo.ppo_onestep`，并把 PPO 适配到 IsaacLab 的 `reset()` / 五元组 `step()` / `obs_dict["policy"]` 接口。
- [ppo_onestep_cfg.py](/home/windsky/project/Clutter/source/Clutter/Clutter/tasks/direct/clutter/agents/ppo_onestep_cfg.py:1)：新增内部 PPO 配置。
- [__init__.py](/home/windsky/project/Clutter/source/Clutter/Clutter/tasks/direct/clutter/__init__.py:15)：注册 `Clutter-Grasp-Direct-v0`。
- [setup.py](/home/windsky/project/Clutter/source/Clutter/setup.py:11)：改为 `find_packages()`，确保新子包安装时可见。

新代码里加了注释/文档字符串，说明 AppLauncher、路径注入、动作维度推断、IsaacLab step 适配、one-step replay、PPO buffer 等关键逻辑含义。

验证情况：

- `python -m py_compile ...` 语法检查通过。
- 新入口和 PPO 模块中没有 `isaacgym`、`isaacgymenvs`、`hydra`、`gymapi`、`gymutil`、`Demograsp` 残留。
- 没有启动完整 IsaacLab 仿真；当前环境运行 `isaaclab_tasks` 会报 `ModuleNotFoundError: No module named 'pxr'`，说明这次 shell 还没有正确拿到 Isaac Sim 的 Python runtime。当前 `clutter_env.py` 也仍是模板环境，真实抓取行为还依赖后续环境层迁移。